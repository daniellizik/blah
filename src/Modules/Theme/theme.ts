import { CLASSIC_COLORS, DARK_COLORS } from '@tablecheck/tablekit-theme';

export const light = {
  colors: {
    ...CLASSIC_COLORS,
    primary: '#0063B0'
  }
};

export const dark = {
  colors: {
    ...DARK_COLORS,
    primary: '#0063B0'
  }
};
